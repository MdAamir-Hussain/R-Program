# Define a list of numbers
numbers <- c(2, 4, 6, 8, 10)

# Calculate the sum of the numbers
total <- sum(numbers)

# Calculate the length of the list
count <- length(numbers)

# Calculate the average
average <- total / count

# Print the average
print(average)
print('hello r program in VS CODE!!!')